"""Tests for `verbio_engine.mouth` — the §8 mouth-layer domain.

P4 L1 covers the pure domain pieces (persona, context, templates,
prompt builder, protocol). Network-bearing implementations
(`DeepSeekMouth`, `TemplatedMouth`) get their own test modules in
later layers.
"""
