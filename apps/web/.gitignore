# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build
/dist

# auto-generated Prisma client (rebuilt by `pnpm db:generate` /
# `prisma generate` during `postinstall` and `build`)
/src/generated/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local env files (keep .env.example committed)
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo

# test
/coverage
/playwright-report
/test-results
# Playwright auth fixture: written by tests/e2e/global-setup.ts on
# every run; the session cookie is per-run noise, not source.
/tests/e2e/.auth/

# editor
.idea
