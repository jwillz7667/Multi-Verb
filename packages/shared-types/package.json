{
  "name": "@verbio/shared-types",
  "version": "0.0.0",
  "private": true,
  "description": "Canonical domain types shared between verbio-web and verbio-engine. Generated from Pydantic.",
  "license": "SEE LICENSE IN ../../LICENSE",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "generate": "node scripts/generate.mjs",
    "check": "tsc --noEmit",
    "lint": "echo \"shared-types is generated; lint runs at workspace root.\" && exit 0",
    "typecheck": "tsc --noEmit",
    "test": "echo \"shared-types has no runtime tests; CI gates on schema freshness.\" && exit 0",
    "clean": "rimraf src/generated"
  },
  "devDependencies": {
    "json-schema-to-typescript": "^15.0.4",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.3"
  }
}
